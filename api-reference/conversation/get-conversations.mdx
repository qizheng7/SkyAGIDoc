---
title: "Get all conversations of a user"
api: "PUT https://api.mintlify.com/api/get-conversations"
description: "This endpoint returns the full list of conversation sessions of a user."
---

### Body

<ParamField body="user_id" type="string">
  This is the ID of a user.
</ParamField>

### Response

<ResponseField name="success" type="number">
  Indicates whether the call was successful. 1 if successful, 0 if not.
</ResponseField>

<ResponseField name="conversations" type="object">
  This is a list of conversation objects.

  <Expandable title="Fields">
  
    <ResponseField name="name" type="string">
      This is the name of a conversation session.
    </ResponseField>

    <ResponseField name="summary" type="string">
      This is the summary of a conversation session.
    </ResponseField>

    <ResponseField name="agents" type="object">
      This is the list of the agents involved in the conversation.
      <Expandable title="Fields">

        <ResponseField name="id" type="string">
        This is the ID of an agent.
        </ResponseField>

        <ResponseField name="name" type="string">
        This is the name of an agent.
        </ResponseField>

      </Expandable>
    </ResponseField>

    <ResponseField name="user_agents" type="object">
      This is the list of the user agents involved in the conversation.
      <Expandable title="Fields">

        <ResponseField name="id" type="string">
        This is the ID of an agent.
        </ResponseField>

        <ResponseField name="name" type="string">
        This is the name of an agent.
        </ResponseField>

      </Expandable>
    </ResponseField>


    <ResponseField name="snapshot" type="object">
      This is the last snapshot of a conversation session.
      <Expandable title="Fields">

        <ResponseField name="agent_id" type="string">
        This is the ID of an agent.
        </ResponseField>

        <ResponseField name="message" type="string">
        This is the message sent from the agent.
        </ResponseField>

      </Expandable>

    </ResponseField>

  </Expandable>

</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request PUT 'https://api.mintlify.com/api/get-conversations' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token <token>' \
--data-raw '{
  "user_id": "xxxxxxxxxxxxxxx"
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": "1",
  "conversations": [
    {
      "name": "Hello world for BigBangTheory",
      "summary": "This conversation is about.....".
      "agents": [
        {
          "id": "xxxxxxxxxxx",
          "name": "Penny"
        },
        {
          "id": "xxxxxxxxxxx",
          "name": "Leonard"
        },
      ],
      "user_agents": [
        {
          "id": "xxxxxxxxxxx",
          "name": "Penny"
        }
      ],
      "snapshot": [
        {
          "agent_id": "xxxxxxxxxxxxxx",
          "message": "Goodbye, Leonard"
        }
      ]
    }
  ]
}
```

</ResponseExample>
