---
title: "Send system message"
api: "PUT https://api.mintlify.com/api/send-system-message"
description: "This endpoint is for asking the initiate agent about whether it wantsto initiate a message to the recipient agent."
---

### Body

<ParamField body="conversation_id" type="string">
  This is the ID of the conversation.
</ParamField>

<ParamField body="initiate_agent_id" type="string">
  This is the ID of the agent who will initiate the conversation.
</ParamField>

<ParamField body="initiate_agent_model" type="object">
  This is the model that the initiate agent uses, along with the model key.
  <Expandable title="Fields">
    <ResponseField name="name" type="string">
      This is the selected model name.
    </ResponseField>

    <ResponseField name="token" type="string">
      This is the model token or API key.
    </ResponseField>

  </Expandable>

</ParamField>

<ParamField body="recipient_agent_id" type="string">
  This is the ID of the agent who receives the conversation.
</ParamField>

<ParamField body="recipient_agent_model" type="object">
  This is the model that the recipient agent uses, along with the model key.
  <Expandable title="Fields">
    <ResponseField name="name" type="string">
      This is the selected model name.
    </ResponseField>

    <ResponseField name="token" type="string">
      This is the model token or API key.
    </ResponseField>

  </Expandable>

</ParamField>

<ParamField body="start_time" type="string">
  This is the start_time of the message history based on which the system will
  create response.
</ParamField>

### Response

<ResponseField name="success" type="number">
  Indicates whether the call was successful. 1 if successful, 0 if not.
</ResponseField>

<ResponseField name="resp_msg" type="object">
  This is the response from the initiate agent to either the system or the
  recipient agent.
  <Expandable title="Fields">
  <ResponseField name="is_valid" type="bool">
    The flag indicating if the response is valid. 
  </ResponseField>

  <ResponseField name="message" type="string">
    The response message.
  </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request PUT 'https://api.mintlify.com/api/send-system-message' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token <token>' \
--data-raw '{
    "conversation_id": "xxxxxxxxxxxx",
    "initiate_agent_id": "xxxxxxxxxxxxxxx",
    "initiate_agent_model": {
      "name": "llama-13b",
      "token": "xxxxxxxxxxxxxxxxx"
    },
    "recipient_agent_id": "xxxxxxxxxxxxx",
    "recipient_agent_model": {
      "name": "gpt-3.5-turbo",
      "token": "xxxxxxxxxxxxxxxxx"
    },
    "start_time": "2023-06-01T01:06:23.415Z"
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": 1,
  "resp_msg": {
    "to_system": "Yes",
    "to_agent": "hello Penny. Do you want to take a walk?"
  }
}
```

</ResponseExample>
